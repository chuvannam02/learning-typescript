import { ApiClient } from '@/utilities-api/apiClient';
import type { {{pascalCase name}}Props } from './{{pascalCase name}}.types';

export class {{pascalCase name}}Service {
    private readonly api: ApiClient;

    constructor(api: ApiClient) {
        this.api = api;
    }

    // example GET
    getAll() {
        return this.api.get<{{pascalCase name}}Props[]>('/{{kebabCase name}}');
    }

    // example POST
    create(data: {{pascalCase name}}Props) {
        return this.api.post<{{pascalCase name}}Props, {{pascalCase name}}Props>('/{{kebabCase name}}', data);
    }
}
